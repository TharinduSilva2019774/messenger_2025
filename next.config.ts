import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["localhost", "https://img.clerk.com", "img.clerk.com","34.134.33.28"],
  },
  /* config options here */
};

export default nextConfig;